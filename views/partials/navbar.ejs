<!-- Flag icon CSS should be included in main layout -->
<style>
    .selected-language {
        font-weight: bold;
        background-color: #f0f0f0;
    }
</style>
<nav class="navbar navbar-dark navbar-expand bg-white shadow mb-4 topbar">
    <div class="container-fluid">
        <button class="btn btn-link d-md-none rounded-circle me-3" id="sidebarToggleTop" type="button">
            <i class="fas fa-bars"></i>
        </button>
        <!-- Home Button (Visible only on mobile) -->
        <button class="btn btn-link d-md-none rounded-circle me-3" id="homeButton" type="button" onclick="window.location.href='/'">
            <i class="fas fa-home"></i>
        </button>
        <form class="d-none d-sm-inline-block me-auto ms-md-3 my-2 my-md-0 mw-100 navbar-search" action="/search" method="GET">
            <div class="input-group">
                <input class="bg-light form-control border-0 small" type="text" placeholder="<%= t('general.search_lyrics') %>" id="searchQuery" name="query" required>
                <button class="btn btn-primary py-0" type="submit">
                    <i class="fas fa-search"></i>
                </button>
            </div>
        </form>
        <ul class="navbar-nav flex-nowrap ms-auto">
            <li class="nav-item dropdown d-sm-none no-arrow">
                <a class="dropdown-toggle nav-link" aria-expanded="false" data-bs-toggle="dropdown" href="#">
                    <i class="fas fa-search"></i>
                </a>
                <div class="dropdown-menu dropdown-menu-end p-3 animated--grow-in" aria-labelledby="searchDropdown">
                    <form class="me-auto navbar-search w-100" action="/search" method="GET">
                        <div class="input-group">
                            <input class="bg-light border-0 form-control small" type="text" placeholder="<%= t('general.search_for') %>" name="query" required>
                            <button class="btn btn-primary" type="submit">
                                <i class="fas fa-search"></i>
                            </button>
                        </div>
                    </form>
                </div>
            </li>
            <% if (hasPermission('view_notifications')) { %>
                <li class="nav-item dropdown no-arrow mx-1">
                    <a class="dropdown-toggle nav-link" aria-expanded="false" data-bs-toggle="dropdown" href="#">
                        <% if (unseenNotificationCount > 0) { %>
                            <span class="badge bg-danger badge-counter">
                                <%= typeof unseenNotificationCount !== 'undefined' && unseenNotificationCount > 0 ? unseenNotificationCount : '' %>
                            </span>
                        <% } %>
                        <i class="fas fa-bell fa-fw"></i>
                    </a>
                        <div class="dropdown-menu dropdown-menu-end dropdown-list animated--grow-in">
                            <div class="dropdown-header d-flex justify-content-between align-items-center px-3 py-2">
                                <span class="text-light medium mb-0"><%= t('general.notifications') %></span>
                                <% if (unseenNotificationCount > 0) { %>
                                    <form action="/notifications/mark-seen" method="POST" class="mb-0">
                                        <button type="submit" class="btn btn-link btn-sm p-0 m-0 text-decoration-none text-light">
                                            <%= t('general.mark_as_read') || 'Mark as read' %>
                                        </button>
                                    </form>
                                <% } %>
                            </div>
                            <% if (notifications && notifications.length > 0) { %>
                                <% notifications.forEach(n => { %>
                                    <div class="dropdown-item d-flex justify-content-between align-items-center">
                                        <a class="d-flex align-items-center text-decoration-none text-dark flex-grow-1" href="<%= n.link || '#' %>">
                                            <div class="me-3">
                                                <div class="bg-primary icon-circle">
                                                    <i class="far fa-bell text-white"></i>
                                                </div>
                                            </div>
                                            <div>
                                                <span class="small text-gray-500">
                                                    <%= new Date(n.created_at).toLocaleString('en-GB') %>
                                                </span>
                                                <p class="mb-0"><%= n.message %></p>
                                            </div>
                                        </a>
                                        <form action="/notifications/mark-seen/<%= n.notification_id %>" method="POST" class="ms-2 mb-0">
                                            <button type="submit" class="btn btn-link btn-sm p-0 m-0 text-decoration-none text-muted" title="Mark as read">
                                                <i class="fas fa-check-circle"></i>
                                            </button>
                                        </form>
                                    </div>
                                <% }) %>
                            <% } else { %>
                                <div class="dropdown-item text-center small text-gray-500">
                                    <%= t('messages.no_notifications') || 'No notifications' %>
                                </div>
                            <% } %>

                            <a class="dropdown-item text-center small text-gray-500" href="#">
                                <%= t('general.show_all_alerts') %>
                            </a>
                        </div>
                </li>
            <% } %>
            <li class="nav-item dropdown no-arrow mx-1">
                <div class="shadow dropdown-list dropdown-menu dropdown-menu-end" aria-labelledby="alertsDropdown"></div>
            </li>
            <div class="d-none d-sm-block topbar-divider"></div>
            <li class="nav-item dropdown no-arrow">
                <div class="nav-item dropdown no-arrow">
                    <a class="dropdown-toggle nav-link" aria-expanded="false" data-bs-toggle="dropdown" href="#">
                        <span class="d-none d-lg-inline me-2 text-gray-600 small">
                            <%= t('messages.logged_in_as') %>
                            <%= session.user.username %>

                        </span>
                        <% if(userRole === 'Viewer') { %>
                            <img class="border rounded-circle img-profile" src="/img/viewer-icon.jpg"> <% } else if(userRole === 'Moderator') { %>
                                <img class="border rounded-circle img-profile" src="/img/moderator-icon.jpg"> <% } else if(userRole === 'Editor') { %>
                                    <img class="border rounded-circle img-profile" src="/img/editor-icon.jpg"> <% } else if(userRole === 'Admin') { %>
                                        <img class="border rounded-circle img-profile" src="/img/admin-icon.jpg"> <% } %>
                    </a>
                    <div class="dropdown-menu shadow dropdown-menu-end animated--grow-in">
                        <% if(hasPermission('access_settings')) { %>
                        <a class="dropdown-item" href="/settings">
                            <i class="fas fa-cogs fa-sm fa-fw me-2 text-gray-400"></i>&nbsp;<%= t('general.settings') %>
                        </a>
                        <a class="dropdown-item" href="/audit-log">
                            <i class="fas fa-list fa-sm fa-fw me-2 text-gray-400"></i>&nbsp;<%= t('general.audit_log') %>
                        </a>
                        <div class="dropdown-divider"></div>
                        <% } %>
                        <a class="dropdown-item" href="/auth/logout">
                            <i class="fas fa-sign-out-alt fa-sm fa-fw me-2 text-gray-400"></i>&nbsp;<%= t('general.logout') %>
                        </a>
                        <!-- Language Dropdown Added Here -->
                        <div class="dropdown-divider"></div>
                        <a class="dropdown-item selected-language" id="lang-en-mobile" href="/change-language/en">
                            <span class="flag-icon flag-icon-gb"></span> <%= t('messages.english') %>
                        </a>
                        <a class="dropdown-item" id="lang-ro-mobile" href="/change-language/ro">
                            <span class="flag-icon flag-icon-ro"></span> <%= t('messages.romanian') %>
                        </a>
                        <a class="dropdown-item" id="lang-it-mobile" href="/change-language/it">
                            <span class="flag-icon flag-icon-it"></span> <%= t('messages.italian') %>
                        </a>
                        <a class="dropdown-item" id="lang-es-mobile" href="/change-language/es">
                            <span class="flag-icon flag-icon-es"></span> <%= t('messages.spanish') %>
                        </a>
                    </div>
                </div>
            </li>
        </ul>
    </div>
</nav>

<script>
    document.addEventListener('DOMContentLoaded', () => {
        // Example function to get the current language from a cookie
        function getCurrentLanguage() {
            const cookies = document.cookie.split(';');
            for (let cookie of cookies) {
                let [name, value] = cookie.split('=');
                if (name.trim() === 'i18next') {
                    return value;
                }
            }
            return 'en'; // Default language
        }

        // Set the flag and highlight the selected language
        const currentLang = getCurrentLanguage();
        const langIcon = document.querySelector('.flag-icon');
        const selectedClass = 'selected-language';
        
        if (currentLang === 'ro') {
            document.getElementById('lang-ro-mobile').classList.add(selectedClass);
            document.getElementById('lang-en-mobile').classList.remove(selectedClass);
        } else {
            document.getElementById('lang-en-mobile').classList.add(selectedClass);
            document.getElementById('lang-ro-mobile').classList.remove(selectedClass);
        }
    });
</script>
