<head>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/flag-icon-css/3.4.3/css/flag-icon.min.css" />
</head>
<nav class="navbar navbar-expand bg-white shadow mb-4 topbar">
    <div class="container-fluid"><button class="btn btn-link d-md-none rounded-circle me-3" id="sidebarToggleTop" type="button"><i class="fas fa-bars"></i></button>
        <!-- Home Button (Visible only on mobile) -->
        <button class="btn btn-link d-md-none rounded-circle me-3" id="homeButton" type="button" onclick="window.location.href='/'">
            <i class="fas fa-home"></i>
        </button>
        <form class="d-none d-sm-inline-block me-auto ms-md-3 my-2 my-md-0 mw-100 navbar-search" action="/search" method="GET">
            <div class="input-group">
                <input class="bg-light form-control border-0 small" type="text" placeholder="<%= t('general.search_lyrics') %>" id="searchQuery" name="query" required>
                <button class="btn btn-primary py-0" type="submit">
                    <i class="fas fa-search"></i>
                </button>
            </div>
        </form>
        <ul class="navbar-nav flex-nowrap ms-auto">
            <li class="nav-item dropdown d-sm-none no-arrow"><a class="dropdown-toggle nav-link" aria-expanded="false" data-bs-toggle="dropdown" href="#"><i class="fas fa-search"></i></a>
                <div class="dropdown-menu dropdown-menu-end p-3 animated--grow-in" aria-labelledby="searchDropdown">
                    <form class="me-auto navbar-search w-100" action="/search">
                        <div class="input-group">
                            <input class="bg-light border-0 form-control small" type="text" placeholder="<%= t('general.search_for') %>" name="query" required>
                            <button class="btn btn-primary" type="submit">
                                <i class="fas fa-search"></i>
                            </button>
                        </div>
                    </form>
                    
                </div>
            </li>
            <li class="nav-item dropdown no-arrow mx-1">
                <a class="dropdown-toggle nav-link" aria-expanded="false" data-bs-toggle="dropdown" href="#">
                    <span class="flag-icon flag-icon-ro"></span> <!-- Show current language flag -->
                </a>
                <div class="dropdown-menu dropdown-menu-end animated--grow-in">
                    <h6 class="dropdown-header"><%= t('general.current_language') %></h6>
                    <a class="dropdown-item" href="/change-language/en">
                        <span class="flag-icon flag-icon-gb"></span> English
                    </a>
                    <a class="dropdown-item" href="/change-language/ro">
                        <span class="flag-icon flag-icon-ro"></span> Romanian
                    </a>
                </div>
            </li>
            <li class="nav-item dropdown no-arrow mx-1">
                <div class="nav-item dropdown no-arrow"><a class="dropdown-toggle nav-link" aria-expanded="false" data-bs-toggle="dropdown" href="#"><span class="badge bg-danger badge-counter">3+</span><i class="fas fa-bell fa-fw"></i></a>
                    <div class="dropdown-menu dropdown-menu-end dropdown-list animated--grow-in">
                        <h6 class="dropdown-header"><%= t('general.notifications') %></h6><a class="dropdown-item d-flex align-items-center" href="#">
                            <div class="me-3">
                                <div class="bg-primary icon-circle"><i class="far fa-bell text-white"></i></div>
                            </div>
                            <div><span class="small text-gray-500">December 12, 2019</span>
                                <p>New song requested:<br>De ziua aceasta iti doresc (PDC)</p>
                            </div>
                        </a><a class="dropdown-item text-center small text-gray-500" href="#"><%= t('general.show_all_alerts') %></a>
                    </div>
                </div>
            </li>
            <li class="nav-item dropdown no-arrow mx-1">
                <div class="shadow dropdown-list dropdown-menu dropdown-menu-end" aria-labelledby="alertsDropdown"></div>
            </li>
            <div class="d-none d-sm-block topbar-divider"></div>
            <li class="nav-item dropdown no-arrow">
                <div class="nav-item dropdown no-arrow"><a class="dropdown-toggle nav-link" aria-expanded="false" data-bs-toggle="dropdown" href="#"><span class="d-none d-lg-inline me-2 text-gray-600 small"><%= userRole %></span><img class="border rounded-circle img-profile" src="/img/admin-icon.svg"></a>
                    <div class="dropdown-menu shadow dropdown-menu-end animated--grow-in"><a class="dropdown-item" href="#"><i class="fas fa-user fa-sm fa-fw me-2 text-gray-400"></i>&nbsp;<%= t('general.profile') %></a><a class="dropdown-item" href="/settings"><i class="fas fa-cogs fa-sm fa-fw me-2 text-gray-400"></i>&nbsp;Settings</a><a class="dropdown-item" href="#"><i class="fas fa-list fa-sm fa-fw me-2 text-gray-400"></i>&nbsp;Activity log</a>
                        <div class="dropdown-divider"></div><a class="dropdown-item" href="/auth/logout"><i class="fas fa-sign-out-alt fa-sm fa-fw me-2 text-gray-400"></i>&nbsp;<%= t('general.logout') %></a>
                    </div>
                </div>
            </li>
        </ul>
    </div>
</nav>

<script>
    document.addEventListener('DOMContentLoaded', () => {
    // Example function to get the current language from a cookie
    function getCurrentLanguage() {
        const cookies = document.cookie.split(';');
        for (let cookie of cookies) {
            let [name, value] = cookie.split('=');
            if (name.trim() === 'i18next') {
                return value;
            }
        }
        return 'en'; // Default language
    }

    // Set the flag based on the current language
    const currentLang = getCurrentLanguage();
    const langIcon = document.querySelector('.flag-icon');
    if (currentLang === 'ro') {
        langIcon.classList.remove('flag-icon-us');
        langIcon.classList.add('flag-icon-gb');
    } else {
        langIcon.classList.remove('flag-icon-ro');
        langIcon.classList.add('flag-icon-gb');
    }
});
</script>